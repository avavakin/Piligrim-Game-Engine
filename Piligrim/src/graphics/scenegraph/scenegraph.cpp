#include "scenegraph.h"
/*
void piligrim::graphics::SceneGraph::applyMatrix(math::mat4 matr)
{
	for (auto& object : objects)
	{
		object.applyMatrix(matr);
	}
}

void piligrim::graphics::SceneGraph::addObject(const ISceneObject &object)
{
	objects.push_back(object);
}

void piligrim::graphics::SceneGraph::clearScene()
{
	objects.clear();
}

void piligrim::graphics::SceneGraph::draw()
{
	for (auto& object : objects)
	{
		object.draw();
	}
}
*/
